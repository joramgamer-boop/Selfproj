# Context

There is no repo-wide glossary. Domain terms live in the project that owns them:
see `<project>/CONTEXT.md` — for example, `tic-tac-toe/CONTEXT.md`.

Scoped per project, since the projects in this repo share no domain. A term
defined for one project says nothing about another.
