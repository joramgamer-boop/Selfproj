// @ts-check
import { defineConfig } from 'astro/config';

// One static page, built from content files in this folder. `site` and `base`
// arrive with the GitHub Pages ticket; nothing here depends on them yet.
export default defineConfig({
  output: 'static',
});
